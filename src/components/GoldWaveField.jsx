/**
 * Quiet modern salon base — warm cream only.
 * Photography carries the wow in the hero; no busy ribbon/atmosphere layers.
 */
export default function GoldWaveField() {
  return <div className="gold-wave-field salon-atmosphere" aria-hidden="true" />;
}
